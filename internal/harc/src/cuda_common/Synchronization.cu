#include <cstdint>

namespace harc::cuda {

    ///
    /// \param buffer Pointer to buffer large enough to contain
    /// \param value
    /// \return
    [[nodiscard]]
    std::uint32_t exclusive_prefix_sum_across_grid(std::uint32_t* buffer, std::uint32_t value) {


        return 0;
    }

}
