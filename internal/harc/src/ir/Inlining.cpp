#include <harc/ir/IR.hpp>

namespace harc::ir {

    bool should_inline(const Function& caller, const Function& callee) {
        // Check if the caller is very short


        // Check if caller is very short


    }

}
